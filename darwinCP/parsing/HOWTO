fichiers importants  (source): lexer.mll (lex) parser.mly ( yacc) calc.ml (conbination)     
il faut ajouter les fichiers compilé de type.ml
pour tout compiler lancer ça : 

        ocamllex lexer.mll       # generates lexer.ml
        ocamlyacc parser.mly     # generates parser.ml and parser.mli
        ocamlc -c parser.mli
        ocamlc -c lexer.ml
        ocamlc -c parser.ml
        ocamlc -c calc.ml
        ocamlc -o calc lexer.cmo parser.cmo calc.cmo
